return {
  'royanirudd/clipboard-history.nvim',
  opts = {
    max_history = 200, -- Optional: set max history (default 100)
    enable_wsl_features = true, -- Set to true if you're using WSL and want Windows clipboard integration
  }
}

